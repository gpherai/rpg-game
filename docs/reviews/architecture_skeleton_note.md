# Architectuur-skelet update

- `pyproject.toml` ingevuld met PEP 621-velden, runtime-dependency `pygame>=2.5`, dev-extra met `pytest`, `ruff`, `mypy`, `black`, Python >=3.11, hatchling build.
- `src/tri_sarira_rpg/core/stat_blocks.py` tijdelijk verwijderd; hoort later terug te keren onder juiste systems-/domeinlaag zodra de statstructuur wordt ingevuld.
